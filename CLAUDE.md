# ipfs-test

**Resuming work? Read [PLAN.md](PLAN.md) first.** It is the current working plan:
what's shipped, the GitHub-Pages + PubSub-only pivot we're moving toward, the
Private Network Access test (`pna-probe.html`) that gates that pivot, and the
ordered next steps (room-param join + invite generator are safe to build now;
the deploy and IPNS/publishing retirement wait on the PNA result).

This is a **mini-spec** project — load the `mini-spec` skill before writing or
modifying any code. The `specs/` → `design/` → code chain (with `// CRC:`/`Seq:`
traceability comments) is load-bearing; don't edit code without it.
