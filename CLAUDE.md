# ipfs-test

**Resuming work? Read [PLAN.md](PLAN.md) first.** It is the current working plan:
what's shipped (the GitHub-Pages + PubSub-only pivot has landed), the one gap
still in flight (O3, the manual end-to-end run that automated browsers can't
do), the ordered next steps, and the key facts about local-network-access
detection so they don't get re-derived.

This is a **mini-spec** project — load the `mini-spec` skill before writing or
modifying any code. The `specs/` → `design/` → code chain (with `// CRC:`/`Seq:`
traceability comments) is load-bearing; don't edit code without it.
